"""Rotate command (placeholder)."""
import sys


def rotate_command(vault_path: str, mode: str, shares: list = None) -> int:
    """Rotate shares or passphrase."""
    print("Rotate command not yet fully implemented", file=sys.stderr)
    return 9
